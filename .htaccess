# Apache .htaccess file for redirects
# Redirect .html versions to clean URLs

RewriteEngine On

# Redirect root index.html to root
RewriteRule ^index\.html$ / [R=301,L]

# Redirect articles/index.html to articles/
RewriteRule ^articles/index\.html$ /articles/ [R=301,L]
